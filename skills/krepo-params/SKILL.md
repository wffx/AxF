# krepo-params

## 目标

复用 kRepo 参数约束分析能力，为目标函数生成入参约束文本。

## 输入

- `repo`
- `function`
- `file`
- `db`
- `artifact_dir`

## 输出

- `params_text`

## 约束

- 输出必须写入 AxF run 的 `artifacts/krepo/params.txt`。
